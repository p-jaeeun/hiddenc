import React from 'react';
import AuthForm from '../components/AuthForm';

const Login = () => {
  return (
    <React.Fragment>
      <AuthForm />
    </React.Fragment>
  );
};

export default Login;
