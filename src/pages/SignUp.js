import React from 'react';
import AuthForm from '../components/AuthForm';

const SignUp = () => {
  return (
    <React.Fragment>
      <AuthForm authType="회원가입" />
    </React.Fragment>
  );
};

export default SignUp;
